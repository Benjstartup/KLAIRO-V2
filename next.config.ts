import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Phase 0: minimal config
  // Market data image domains, CSP headers, etc. will be added in Phase 1+
};

export default nextConfig;
